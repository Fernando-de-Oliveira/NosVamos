$(document).ready(() => {
    // $('#redirectToMap').on('click', () => {
    //         $(location).attr('href', '/mapa.php')
    //     })
    $('.menu__iconTeste').on('click', () => {
        $('.section__menu').toggleClass('is-active');
    })
})